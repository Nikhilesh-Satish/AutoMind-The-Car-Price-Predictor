const defaultData = {
  vehicle_age: 0,
  km_driven: 0,
  mileage: 0,
  engine: 0,
  max_power: 0,
  seats: 0,
  seller_type:"",
  // Brands (28)
 brand:"",

  // Fuel Types (4)
 fuel_type:"",

  // Transmission (1 because drop_first=True)
  transmission_type: ""
};

export default defaultData;
